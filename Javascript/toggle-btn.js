function toggle () {
    //Select slider element from html
    var slider = document.getElementById('slider');

    //Condition: If activated, float right & change background color to green
    if () {
        slider.style.color = 'green';
    }
    //Condition: Otherwise, float left & change background to grey
    else {
        slider.style.color = 'grey';
    }

}